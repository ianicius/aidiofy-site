// API Services have been removed for the landing page version.
export const extractTextFromImage = async () => { throw new Error("Service unavailable"); }
export const generateSpeechFromText = async () => { throw new Error("Service unavailable"); }